<% provide(:button_name, "Edit Admin" )%>

<%= turbo_frame_tag(dom_id(@admin)) do %>
  <div class="flex flex-col justify-center items-center">
    <div class="text-2xl pt-8">Edit <%= @admin.full_name %>'s profile</div>
    <%= render "admins/form", admin: @admin %>
  </div>
<% end %>